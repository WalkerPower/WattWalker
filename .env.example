# Gemini (bill analysis) — create at https://aistudio.google.com/apikey
# Server-side key (used by FastAPI on port 8080 and Cloud Run):
GEMINI_API_KEY=

# Optional client fallback when the backend is not running (local dev only):
# VITE_GEMINI_API_KEY=

# Production Cloud Run URL (Capacitor / mobile builds):
# VITE_API_BASE_URL=https://your-wattwalker-service.run.app
